{
  "name": "global-market-cloudflare",
  "version": "5.6.3",
  "private": true,
  "description": "GLOBAL MARKET - fluidité cloud renforcée : lecture stale-while-revalidate, cache KV de secours, file d’écriture D1 résiliente et reprise automatique sans interruption.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "npm run validate && node scripts/build.mjs",
    "check": "npm run build",
    "dev": "npm run build && wrangler pages dev public",
    "deploy": "npm run build && wrangler pages deploy public --project-name=globalmarketci"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
