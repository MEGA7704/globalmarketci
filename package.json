{
  "name": "global-market-cloudflare",
  "version": "4.6.0",
  "private": true,
  "description": "GLOBAL MARKET - espace client marketplace premium, suivi des commandes, paiement à la livraison sans identifiant et rapport clients avancé.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "npm run validate && node scripts/build.mjs",
    "check": "npm run build",
    "dev": "npm run build && wrangler pages dev public",
    "deploy": "npm run build && wrangler pages deploy public --project-name=globalmarketci"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
