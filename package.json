{
  "name": "global-market-cloudflare",
  "version": "5.3.0",
  "private": true,
  "description": "GLOBAL MARKET - moyens d’expédition intégrés à l’adresse de livraison, villes sans frais fixes, retrait boutique verrouillé et panier client simplifié.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "npm run validate && node scripts/build.mjs",
    "check": "npm run build",
    "dev": "npm run build && wrangler pages dev public",
    "deploy": "npm run build && wrangler pages deploy public --project-name=globalmarketci"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
