{
  "name": "global-market-cloudflare",
  "version": "5.5.1",
  "private": true,
  "description": "GLOBAL MARKET - panier en 3 sections horizontales, frais d’expédition automatiques par moyen et minimum de 10 000 FCFA sur le total général de chaque commande hors ville.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "npm run validate && node scripts/build.mjs",
    "check": "npm run build",
    "dev": "npm run build && wrangler pages dev public",
    "deploy": "npm run build && wrangler pages deploy public --project-name=globalmarketci"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
