{
  "name": "global-market-cloudflare",
  "version": "5.0.0",
  "private": true,
  "description": "GLOBAL MARKET - menus distincts par rôle, compte client global, panier unique multi-boutiques et répartition automatique des commandes par administrateur.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "npm run validate && node scripts/build.mjs",
    "check": "npm run build",
    "dev": "npm run build && wrangler pages dev public",
    "deploy": "npm run build && wrangler pages deploy public --project-name=globalmarketci"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
