{
  "name": "global-market-cloudflare",
  "version": "5.1.0",
  "private": true,
  "description": "GLOBAL MARKET - suppression des pages d'exemple et de l'ancienne connexion, catalogue réel cliquable avec détail produit, panier client unique multi-boutiques et liens légaux.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "npm run validate && node scripts/build.mjs",
    "check": "npm run build",
    "dev": "npm run build && wrangler pages dev public",
    "deploy": "npm run build && wrangler pages deploy public --project-name=globalmarketci"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
