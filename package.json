{
  "name": "global-market-cloudflare",
  "version": "3.8.0",
  "private": true,
  "description": "GLOBAL MARKET - sauvegarde anti-503 : documents D1 par entreprise, lots limités, file d’attente et reprises automatiques.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "npm run validate && node scripts/build.mjs",
    "check": "npm run build",
    "dev": "npm run build && wrangler pages dev public",
    "deploy": "npm run build && wrangler pages deploy public --project-name=globalmarketci"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
