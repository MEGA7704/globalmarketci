{
  "name": "global-market-cloudflare",
  "version": "4.5.0",
  "private": true,
  "description": "GLOBAL MARKET - boutique officielle pleine page, bannière compacte, formulaire de commande défilable, pagination de 16 offres, paiement et livraison sécurisés.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test:isolation": "node scripts/test-isolation.mjs",
    "test:e2e": "node scripts/test-browser-smoke.mjs",
    "build": "npm run validate && npm run test:isolation && node scripts/build.mjs",
    "check": "npm run build && npm run test:e2e",
    "dev": "npm run build && wrangler pages dev public",
    "deploy": "npm run build && wrangler pages deploy public --project-name=global-market"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
