{
  "name": "global-market-cloudflare",
  "version": "5.4.0",
  "private": true,
  "description": "GLOBAL MARKET - quartiers locaux préremplis, retrait boutique à 0 FCFA, expédition hors ville sans pourcentage et résumé panier intégré à l’adresse.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "npm run validate && node scripts/build.mjs",
    "check": "npm run build",
    "dev": "npm run build && wrangler pages dev public",
    "deploy": "npm run build && wrangler pages deploy public --project-name=globalmarketci"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
